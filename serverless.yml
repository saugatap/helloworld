service: helloworld

provider:
  name: aws
  runtime: nodejs8.11
  stage: dev
  region: ap-southeast-1
  
plugins:
  - serverless-sam

functions:
  hello:
    handler: handler.hello
    events:
      - http:
          path: /
          method: ANY
          cors: true